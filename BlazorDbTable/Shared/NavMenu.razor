<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Employees Performance</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="Employees" Match="NavLinkMatch.All">
                Employees
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="EMails" Match="NavLinkMatch.All">
                EMails
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="Performance" Match="NavLinkMatch.All">
                Performance
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="HirarchialTree" Match="NavLinkMatch.All">
                Hirarchy
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="Duplicates" Match="NavLinkMatch.All">
                Duplicates
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
